dist: focal

language: node_js

node_js: '18'

cache: npm

install: npm ci

script:
    - npm run lint
    - npm run build

deploy:
    - provider: script
      script: ./scripts/internal_travis_trigger.sh
      skip_cleanup: true
      'on':
          branch: master
